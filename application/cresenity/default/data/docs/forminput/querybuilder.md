# Form Input - Query Builder

Visual query builder for creating filter conditions with a drag-and-drop interface.

---

### Basic Usage

```php
$form->addField()->setLabel('Filter')->addQueryBuilderControl('filter');
```
