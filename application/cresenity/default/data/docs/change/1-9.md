# Change Log 1.9

## CF

- Narrow `c::config()`, `c::session()`, `c::container()` and `c::theme()` return types with conditional `@phpstan-return`, so they resolve to their object type instead of collapsing to `mixed`
- Remove the dead `checkUnusedViews` PHPStan parameter - it was declared but read by no class, so it only suggested a check was running
- Register `CQC_Phpstan_Service_MacroMethodsClassReflectionExtension`, so PHPStan no longer reports every `::macro()`-defined method as undefined
- Fix `CRouting_RouteFinder::find()` throwing a `LogicException` (500) when a URL's method segment contains repeated `{varname}`-shaped text (e.g. `${IFS}` twice) - it was spliced unsanitized into the compiled route pattern, so Symfony's route compiler misread it as a duplicate route variable
- Fix `phpcf phpcs`'s `--format` option being declared but never passed through to the real `phpcs` binary - now forwards as `--report=<value>`; default changed from the nonexistent `table` report to phpcs' real default, `full`, so existing CLI usage without `--format` is unaffected
- Fix `CQueue_Worker::runNextJob()` enforcing no timeout, so a frozen job held its caller forever
- Rework `CQueue_Worker`: new worker and job events, `CQueue_WorkerStopReason` on every stop, `CQueue_Exception_TimeoutExceededException` for timeouts, and a queue pausing API on `CQueue_Manager`
- Add `starting()` to `CQueue_Contract_MonitorInterface`, matching `Illuminate\Contracts\Queue\Monitor`
- Add `CQueue_QueueRoutes` — route jobs to a connection/queue from one table, matched by class, parent, interface, or trait
- Add `CException_ExceptionHandler::dontRetry()`/`shouldStopRetries()` — an exception type or callback that fails a queued job outright instead of retrying it
- Replace deprecated `${var}` interpolation in `CDaemon` and `CQueue` (28 places)
- Fix `CDaemon_ServiceAbstract` deleting the pid file of a daemon that is still running when a second instance fails to start
- Raise the `CQueue_Runner` default `timeout` from 300 to 1800 seconds, so the fix above catches frozen jobs without killing merely slow ones
- Add `tests/Queue` and `tests/Daemon` suites (46 tests)
- Fix `CModel_Scout_EngineManager::createTntsearchEngine()` reading `database.default` as if it were the connection config
- Fix `CModel_Nested_NestedSet::isNode()` checking the wrong trait, which broke every nested-set relation
- Fix `CConfig::has()` not prefixing its key with the config group
- Fix `CException_ExceptionHandler` reporting its own file and line as the crash site for non-HTTP exceptions
- Fix `CManager_DataProvider_ModelDataProvider` silently losing its `queryCallback`
- Fix a "Failed to retrieve the reflection object" crash in `CFunction_SerializableClosure_Serializer_NativeSerializer`
- Fix `CImage_Avatar_Input_Initials::detectUrlBasedParameters()` fatal outside an HTTP request
- Fix `CApp::renderNavigation()` never applying the navigation callback
- Fix `CApp_Navigation_Helper::nav()` fatal outside an HTTP request
- Navigation `subnav` may be a callable, resolved lazily and memoised per request (`CNavigation_Data::resolveSubnav()`, `hasSubnav()`)
- Nav active state also compares the real request path, and `aliases` accept `*` wildcards
- Add `CApi_MethodAbstract::fail($message, $errCode = null)`
- `CApp_Log_Activity::populate()` accepts app-owned columns and returns the model
- Remove `CApp_Api` and `CApp_Administrator` — both had bypassable authentication; `CApp_Administrator_Fixer_Database` moved to `CDatabase_Fixer`
- Revert the per-app public docroot feature (`CF::publicPath()`, `CHTTP_FileServeDriver`, `asset:install`)
- Remove `CGitlab` from `modules/`, add `GitLabPHP/Client` to `system/vendor/Gitlab`
- Move `LanguageCode/ISO6391.php` from `modules/cresenity/data/` to `system/data/`
- Move the `blockly` asset definition into `system/data/assets-module.php`
- PHP 8.4 deprecation cleanup in `CDatabase_Query_Grammar`, `CSession_Factory`, `CTrait_Compat_Element_DataTable`, `CSession_NativeAdapter`, and vendored Guzzle and League OAuth2
- PHP 8.4 deprecation cleanup: mark 487 nullable-by-default parameters explicitly nullable (`Type $x = null` -> `?Type $x = null`) across `system/libraries`, `core`, `helpers`, and `data`, mechanically, `system/vendor` left alone
- Complete the `Opis\Closure\SerializableClosure` migration audit; fix `c::value()` never invoking a `CFunction_SerializableClosure`, and the same gap in `CElement_FormInput_SelectSearch_Trait_Select2v23Trait`
- Maintenance mode: `down` defaults to `false` instead of `true`, so a malformed `down.php` no longer takes the application down
- Maintenance mode: `down.php` accepts a `secret` key for a bypass link, compared with `hash_equals()`
- Extract `CF::maintenanceDecision($data, $path, $cookie)` as a pure function, covered by `tests/Core/MaintenanceTest.php` (15 tests)
- Add `tests/Translation/` (108 tests); fix a dead `en_US/filemanager.php` stub and a stale placeholder in `id_ID/core.php`; remove `system/i18n/en/`
- Add `tests/Function/SerializableClosureTest.php`, `tests/Function/OpisSerializableClosureTest.php`, `tests/Manager/ModelDataProviderTest.php`, `tests/Ajax/SelectSearch{Model,Closure}RoundTripTest.php`, `tests/Pagination/*`, and `tests/Config/CConfigTest.php`
- Investigated a reported `CElement_FormInput_EditorJs_Tool_ImageTool` crash — not reproducible, no fix needed
- `phpcf devcloud:team:switch` prompts with an interactive team picker when `teamCode` is omitted, marking the current team

- Fix `CDatabase_Schema_Builder_PostgresBuilder` fataling the moment it loaded — `parseSchemaAndTable()` was `protected` while the base declares it `public`, so the whole Postgres schema builder was unusable
- Drop three stale `PostgresBuilder` overrides that no longer matched the base: `parseSchemaAndTable()` returned three elements where callers destructure two, `hasTable()` relied on that shape, and `getColumnListing()` called a `processColumnListing()` that exists on no processor
- Fix `CDatabase_Schema_Grammar::compileCreateDatabase()`/`compileDropDatabaseIfExists()` docblocks promising `@throws \LogicException` and `@return void` while both return SQL

- Fix `tests/Database/QueryDurationThresholdTest.php` importing `CarbonV3\CarbonInterval` while the framework's own Carbon is `Carbon\Carbon` — `whenQueryingForLongerThan()` never recognised the interval and compared an object against a float

- Fix `DatabaseConnectionFactoryTest` reaching the app's real MySQL instead of its own in-memory sqlite — `Manager::configuration()` treats the connection name `default` as an alias for `database.default`, so a connection registered under that name can never win
- Add `CDatabase_Connection::$compileQueryLog` — the query log's `compiled` key runs every binding through `PDO::quote()` on the write connection, so logging a read query can open a write connection for nothing; set it false to skip that, and the key stays present as null

- `CommandTest`, `DatabaseConnectionTest`, and `ModelBelongsToTest` use Mockery's PHPUnit integration trait, so their mock expectations count as assertions instead of being reported as tests that assert nothing

- Add `tests/Container/` (100 tests across 6 files) — binding and resolution, `call()`, extenders, tagging, non-instantiable targets, contextual binding, and resolving callbacks
- Fix `CContainer_BoundMethod::getCallReflector()` rejecting an invokable object, so `call($object)` could never reach its `__invoke`
- `CContainer_Container::when()` accepts a list of consumers, not only one
- Fix `CContainer_Container::build()` letting a raw `ReflectionException` escape for a class that does not exist, so a caller guarding against `CContainer_Exception_BindingResolutionException` missed it entirely

- Fix `CDatabase_Schema_Grammar_PostgresGrammar` using `throw` as an expression, which PHP 7.4 cannot parse — the whole file was unusable there, taking 40 tests with it
- Fix `CModel_Relation_HasOneOrManyThrough::createOrFirst()` using `?? throw`, a PHP 8.0 throw expression — PHP 7.4 fails to parse the whole file, so every has-one/has-many-through relation dies with it, not just this one method
- Fix `carr::implodes()` returning `false` instead of `''` for an empty array on PHP 7.4, where `substr('', 0, -1)` still answers `false`

- Add `tests/Queue/` coverage for the sync driver, `CQueue_Manager`, queue pausing, the queue exceptions, and the `InteractsWithQueue` trait (5 files, 45 tests)
- Fix `CQueue_Manager::resolve()` reading `$config['driver']` on a null config, so a misspelled connection name surfaced as "Trying to access array offset on value of type null" instead of naming the connection

- Add `tests/Queue/QueueSqsQueueTest.php` and `QueueNullQueueTest.php` — the SQS driver against a mocked client, and the null driver's guarantee that everything stays a quiet no-op

- The `CQueue_Runner` default `timeout` stays at 300 seconds here; the 1.6 branch deliberately uses 1800 because its `once` path never enforced a timeout before, and a test now pins the difference so a forward merge cannot carry it over unnoticed

- Fixed `<extensions>` in `phpunit.xml` never loading: the handler asked for an interface under a namespace the vendored tree no longer uses, so every registered hook was rejected as not being a hook

- Fixed `assertContainsOnlyInstancesOf()` fataling with a class-not-found error: the constraint referenced `IsInstanceOf` without importing it

- Replaced every `"${var}"` string interpolation with `"{$var}"` (291 of them across 67 files); the old form is deprecated on PHP 8.2 and gone in PHP 9

- Fixed the PHP 8.1 null-argument deprecations that fired on every request: `cstr::contains()` now casts its haystack, `CView_Factory::compiledPath()` its app code, and `CFConfig::bootstrap()` no longer calls `file_exists()` with no cached path

- Added `#[\ReturnTypeWillChange]` to the 250 interface methods (`count()`, `getIterator()`, `offsetGet()`, `jsonSerialize()`, …) that PHP 8.1 warns about, so a framework run under `error_reporting=-1` is quiet again
- Replaced the deprecated `ReflectionParameter::getClass()` in the container and the route dependency resolver with `CBase_Reflector::getParameterClassName()`
- Replaced `strftime()` in the captcha riddles with `date()`; it is deprecated on PHP 8.1 and gone in PHP 9

- Added `CQueue_Trait_ResolvesQueueRoutesTrait`, used by `CQueue_Manager` and `CQueue_Dispatcher`; binding `queue.routes` in the container now replaces the routing table without touching the shared one

- An application may now declare the applications it shares code with in `application/<app>/shared.php`; it is read before configuration is loaded, so a config file may refer to a class of the shared application. `CF::addSharedApp()` from the application bootstrap runs too late for that
- Restored `CConfig::refresh()`, dropped when configuration became eagerly loaded; an application still calling it died at boot
- Removed a stray `cdbg::dd()` at the top of `CConfig_Loader::load()` that killed the process on every call

- Fixed a closure with an untyped parameter killing the request in `CTrait_ReflectsClosureTrait`; it now reports the parameter as untyped instead of calling a method on null

- Add `phpcf devcloud:project:create`, `devcloud:git:create`, `devcloud:app:create`, `devcloud:database:create-dev`, and `devcloud:app:scaffold` — create/register a devcloud project, git, app and seeded dev database from the CLI
- Add `phpcf devcloud:server:command:list`/`devcloud:server:command:run` — list/run a server's stored SSH commands from the CLI
- Add `phpcf devcloud:server:list`/`devcloud:server:status` — list servers, and show a server's live system/resource info plus registered services' status, from the CLI
- Add `phpcf devcloud:server:webserver`/`devcloud:server:vhost:list` — detect which webserver(s) are installed/running on a server, and list its virtual hosts, from the CLI
- Add `phpcf devcloud:server:service:control` — start/stop/restart a server's registered service from the CLI

## CServer

- Add `CServer_OSAbstract` with `CServer_OS_{Debian,RedHat,Unknown}`, detected from the remote `/etc/os-release`; reachable as `CServer::server($ssh)->distro()`
- Add `CServer_Certbot` (`CServer::server($ssh)->certbot()`) — install, list, issue, renew, and delete certificates
- Add `CServer_Redis` (`CServer::server($ssh)->redis()`) — version, service status, `INFO`, keyspace, and `getWarningList()`
- Add `CServer_WebServer` (`CServer::server($ssh)->webServer()`) — reports every web server present, its listening ports, virtual hosts, `testConfig()`, and `reload()`
- Add `CServer_WebServer_LiteSpeed` — SSH bridge to `CVendor_LiteSpeed`; reads virtual hosts from `httpd_config.conf` rather than scanning `conf/vhosts/*`
- Add `CServer_Mail` (`CServer::server($ssh)->mail()`) — MTA, MDA, IMAP/POP server, publicly listening mail ports, spam filter, and outbound relay
- Add `CServer_Cf` (`CServer_Server::cf()`) — detect a CF installation on a document root, its apps, and the installed version
- Add SSH key pair generation: `CRemote_SSH_Key` (locally) and `CServer_SshKey` (on the target server, with an idempotent `addAuthorizedKey()`)
- Add `CServer_Php::inspectAll($server)` — discover every PHP on a server in one command; add `CServer_Php::isEndOfLife($version)`
- Add `CServer_WebServer_LiteSpeed::{getPhpAvailabilityList,phpPackageList,installPhp}()`, building the package list and install command from the server's own distro
- Add `CServer_WebServer_LiteSpeed::{writeFile,restart}()`; content is sent as base64 and `main.cf`-style dated backups are written first
- Add `CServer_WebServer::{getVirtualHost,liteSpeed}()` and `isValidVirtualHostName()`
- Add `CServer_WebServer_LiteSpeed::{getExternalAppList,getPhpList}()`; `getListenerList()` splits `address` into ip and port and reports `keyFile`/`certFile`
- Add `CServer_Mail::{getLogTail,getLogSummary,logFilterList}()` — read the mail log with a filter, across rotated and gzipped generations
- Add `CServer_Mail::{getWebmailList,addWebmailDomain}()` — find RainLoop, SnappyMail, or Roundcube and register a domain with it
- Add `CServer_Mail::attachSpamFilter($port = '11332')` — appends to `smtpd_milters` and forces `milter_default_action = accept`
- Add `CServer_Mail::getSpamScannedCount()`, kept out of `inspect()` because `rspamc stat` takes two seconds
- Add `CServer_Mail::{getRelayUsername,getRelaySecret,providerList,testRelay,applyRelay}()` — read and change the outbound relay, with per-stage test results
- Add `CServer_Mail::getSenderDomainList($day)` — domains the server actually sends as, counted from the mail log
- Add `CServer_Mail::getHostedDomainList()` — domains whose mailboxes the machine serves, read from `virtual_mailbox_domains`
- `CServer_Mail::inspect()` also reports the spam filter in three states (`none`/`idle`/`active`), the MDA, and the outbound relay
- Fix `CServer_Mail::applyRelay()` corrupting every relay password it wrote, from an unquoted `printf` format string
- Fix `CServer_SSHRepository` caching connections by `host:port:username` only, so a short timeout silently truncated later long-running commands
- `CServer_Certbot` passes `--no-random-sleep-on-renew`, runs through `sudo -n` on non-root connections, and reports a permission refusal instead of an empty certificate list
- `CServer_Certbot` renewal and deletion are per-certificate; `renew()`, `renewDryRun()`, and `delete()` take a lineage name
- `CServer_Certbot` and `CServer_Redis` consult the distribution for ordering and first candidate, while the existing probe still decides
- `CServer_Server::php()` accepts a binary and caches one instance per binary
- `CServer_Exception` now extends `CException`; add `CServer_Exception_{UnsupportedKeyType,InvalidKeyBits,CommandNotFound,KeyGenerationFailed,InvalidPublicKey,InvalidDomain}Exception`
- Rename Indonesian identifiers in `CServer_Mail`, `CServer_Certbot`, and `CServer_SshKey` to English (`pisahDaftar()` → `splitList()`)

## CVendor

- Upgrade the DigitalOcean SDK from 3.x to 4.9.1 (`system/vendor/DigitalOceanV2`), the last release accepting PHP 7.4
- Add `CVendor_DigitalOcean::fetchAll($api, $method, $parameters = [])` — **use this for every `getAll()`**; 4.x drops `per_page`, so `getAll()` silently truncates at 20 items
- DigitalOcean 4.x renames `delete()` to `remove()` on `droplet`, `volume`, `snapshot`, `floatingIp`, `domain`, `domainRecord`, and `key`, with no deprecation shim
- Every DigitalOcean SDK file declares `strict_types=1`, so a `null` id now raises a `TypeError` where 3.x accepted it
- Remove a stale `use DigitalOceanV2\DigitalOceanV2;` from `CResources_Loader_Image`
- Add `CVendor_Mailjet` — Mailjet REST v3 client with `CVendor_Mailjet_Sender`, `CVendor_Mailjet_Dns`, and `CVendor_Mailjet_Exception`; add the `CVendor::mailjet()` and `CVendor::mailerSend()` factories
- Add `CVendor_LiteSpeed::parseConf($configType, $content)` — parse a config already in memory, deleting its temporary file in a `finally`
- Add `CVendor_LiteSpeed::{nodeSource,nodeBodySource}()` — the original source of a node, because the parse tree regroups children by key and that changes the meaning of `rewrite` blocks
- Add `CVendor_LiteSpeed::expandPath()` — substitute `$SERVER_ROOT`/`$VH_ROOT`; fix relative paths being rooted at `conf/` instead of `$SERVER_ROOT`
- Add `CVendor_LiteSpeed_Node::{getChildList,getChildKeyList,isBlock,getPrintKey,getRawKey}()`; `get()` also serves `FLD_TYPE` and `FLD_PRINTKEY`
- `CVendor_LiteSpeed_Node::get()` throws `InvalidArgumentException` for an unknown field instead of calling `die()`
- Complete the `@param`/`@return` annotations on 41 `CVendor_LiteSpeed_Node` methods
- Replace deprecated `${var}` interpolation across `CVendor/LiteSpeed` (53 lines in 16 files)

## CElement

- Fix the show-password button (`control:Password` with `setToggleVisibility()`) breaking its layout outside Bootstrap 4; it now uses its own `cres-password-group` class
- Fix `CElement_Component_DataTable`'s export traits reading a `$footer_field` property that does not exist — every export with a footer enabled hit a null access
- Convert `CElement_FormInput_Text` to cres.js (`control:Text`); remove the dead `$action`/`add_action()`/`buttonPosition` API
- `CElement_FormInput_FileAjax` extends `CElement_FormInput_File`; `acceptFile` moved to the shared parent and is validated client-side
- Add translated accept-file and upload validation messages via `cres-config` (`element/file`, new `element/upload` i18n group)

## CView

- Fix `CView_Engine_FileEngine` declaring `implements CView_EngineAbstract` on an abstract class — a fatal the moment it loaded, making the `file` view engine unusable
- Fix `CView_Trait_ManageEventTrait::buildClassEventCallback()` referencing an undefined `$this->container`, so string-based view composers always threw
- Add `tests/View/` covering `CView_Compiler_BladeCompiler`, `ComponentTagCompiler`, `Finder`, `EngineResolver`, `ComponentAttributeBag`, `Factory`, `View`, and the component runtime (349 tests)

## CComparator

- Fix `CComparator_Differ_AbstractOutput` declaring `implements CComparator_Differ_OutputBuilder`, a name that does not exist — a fatal on load that made `UnifiedDiffOutput` unusable

## CManager

- Add the `jvectormap` asset module (jVectorMap 3.1.1 with world map data) under `media/{js,css}/plugins/jvectormap/`

## CAnalytics

- Fix `CAnalytics_Google_ClientGA4::runReport()` passing `$cacheInMinutes` straight to `remember()`, which counts seconds; renamed to `$cacheInSeconds`
- Build the report cache key from the requested report only, not from `func_get_args()`
- Add `CAnalytics_Google_AnalyticGA4_PredefinedReport::getTopList($dimension, $metric, $period, $limit, $cache)`
- Add `CAnalytics_Google_AnalyticGA4_PredefinedReport::getTopRealtimeList($dimension, $metric, $limit, $minutes, $cache)`

## cdbutils

- Fix `cdbutils::get_row()` always returning `null` — a stray `return null;` on the first line of the body since 31 August 2023, across 323 call sites in 18 applications

## htaccess

- Block `application/*/default/data/docs/` and every `.md` under `data/` from direct web access
- Block `BUG.md` from direct web access

## CQueue

- Fix `CQueue_FailedJob_DatabaseFailedJob::forget()` filtering on column `id` instead of the table's real primary key (`{table}_id`), so it never deleted anything

## CCron

- Fix `CCron_CacheEventMutex::exists()`/`forget()` referencing an undefined `$this->cache` property instead of `CCache::manager()` (the pattern `create()` already used correctly), so any scheduled job registered with `->withoutOverlapping()` crashed with "Undefined property: $cache" the moment its overlap check ran — merged forward from CF1.6, where it surfaced live on a production `cresenity/cron` tick

## CCache

- Fix `CCache_Driver_ArrayDriver::flush()` clearing `$storage` but not `$locks`, so a lock acquired before `flush()` could never be re-acquired in the same process
- Fix the file cache driver failing to write after `flush()` in the same process — see `CTemporary` below

## CTemporary

- Fix `CTemporary_File::getPath()` building its path by string concatenation instead of going through `CTemporary_Directory::getPath($filename)`, so a directory removed by `CCache_Driver_FileDriver::flush()` (or any other caller of `CTemporary_Directory::delete()`) was never recreated on the next write

## Vendor

- Fix `GuzzleHttp\Handler\CurlMultiHandler`/`GuzzleHttp\Ring\Client\CurlMultiHandler` triggering a PHP 8.2+ "Creation of dynamic property `$_mh`" deprecation on every outbound HTTP call made through Guzzle — declare the property and `unset()` it right after construction so the existing lazy-init-via-`__get()` still runs exactly once

## CDatabase

- Fix `CDatabase_Connection`'s query log always quoting its `compiled` entry through the write connection (`escapeString()` → `getPdo()`), even for a query that ran entirely on the read connection — merely logging a read query could open a write connection just to format the log line. `select()`/`query()`/`selectResultSets()`/`cursor()` now pass their own `$useReadPdo` through `run()` → `logQuery()` → `compileBinds()` → `escape()`, so the compiled entry is quoted against whichever connection actually ran the query; every other caller (`statement()`, `affectingStatement()`, `unprepared()`, and any external `compileBinds()` caller) is unaffected and keeps quoting against the write connection

## CEvent

- Fix `CEvent_Dispatcher::hasListeners()` checking for an exact key in the wildcard listener array instead of pattern-matching it, so a `pesanan.*` listener made `hasListeners('pesanan.dibuat')` answer `false` even though `dispatch('pesanan.dibuat')` really would call it — now delegates to `hasWildcardListeners()`, the same pattern-matching `getListeners()`/`dispatch()` already used

## CDaemon

- Add `CDaemon_ProcessMetrics::forPid()` and `CDaemon_Runner::getProcessMetrics()` — file descriptor usage, process age, and CPU time for a running daemon, read from `/proc/<pid>/fd`, `/proc/<pid>/limits`, and `ps`; a leaking or spinning daemon still reports normal memory and a `Running` status, so neither was visible before
- The sysadmin daemon table (`CTrait_Controller_Application_Manager_Daemon::reloadTableService()`) gets a new "Process" column showing fd/age/cpu per service
- `CDaemon_Supervisor`'s dashboard (`ajaxWorkers()`) reports the same fd/age/cpu per master and per supervisor pool, gated by `CDaemon_ProcessMetrics::belongsToHost()` — a pid registered in Redis by a different host is only meaningful read on that host, so a remote record shows `-` instead of silently reading whatever unrelated local process happens to share that pid
- Fix `CDaemon_ServiceAbstract::restart()` leaking Redis and database sockets into the daemon it execs

## CML

- Add `CML::associationRules($transactions, $minSupport, $minConfidence, $maxItemsetSize)` — Apriori association rule mining over transaction baskets, returning `{antecedent[], consequent, support, confidence, lift}` sorted by lift. Answers "buy A and B, usually also C" (bundling/promo) rather than "what is similar to A"; lift is what separates a real association from an item that is merely popular, since a ubiquitous item scores perfect confidence but lift 1
- Add `CML::itemCollaborativeSimilarity($transactions, $topK, $maxItemPerTransaction)` — item-to-item collaborative filtering ("customers who bought this also bought that") over transaction baskets, using Rubix ML's `Kernels\Distance\Cosine` on each item's occurrence vector. Cosine rather than a raw co-occurrence count so a bestseller does not look related to everything merely by being in most baskets; small baskets are weighted `1/(n-1)`, and only pairs that actually co-occur are computed
- `CML::itemCollaborativeSimilarity()` gains an optional `$minPairSupport` (default `1`, unchanged behaviour) — cosine scores a pair seen in a single basket the same 1.0 as one proven hundreds of times, so a support floor is the only way to keep coincidences out of the top of the list
- Add `CML::textSimilarity($text, $candidates)` — cosine similarity between a text and a list of candidate texts via TF-IDF vectors (already-vendored Rubix ML: `WordCountVectorizer` + `TfIdfTransformer` + `Kernels\Distance\Cosine`), for word-overlap-based near-duplicate detection with no external embedding API or vector DB needed

## CNavigation

- Add section-heading support to `CNavigation_Renderer_SidenavRenderer` — a nav item with `'heading' => true` renders as a static `<li class="sidenav-heading">` label instead of a link; a heading whose following items are all filtered out by access checks is not rendered

## CModel_Scout

- Fix `CModel_Scout_Engine_TNTSearchEngine::initIndex()` calling a non-existent `driver()` on the connection — building a TNTSearch index for the first time always fataled with "Call to undefined method ...::driver()"; now reads the PDO from the connection directly
- `CTrait_Controller_Application_Model_Scout` gets a fuller manager: engine summary (driver, storage path, totals), per-index size/last-modified, an out-of-sync warning, orphan detection (index holding more rows than the table), and a `tester()` action that runs a real query against the live index with selectable all-words/any-word and typo-tolerance — existing `index()`/`import()`/`flush()` and the `getSearchableModels()` contract are unchanged

## CRedis

- Fix `CRedis::purge()` not closing the connection it drops — a persistent (pconnect) socket survives a plain unset
